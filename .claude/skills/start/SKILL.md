---
name: start
description: 작업을 시작한다. GitHub 이슈를 만들고 규칙에 맞는 브랜치를 만들어 착수한다. 사용자가 새 작업을 시작하려 하거나 "이거 작업 시작할게", "브랜치 만들어줘", "이슈부터 만들자" 라고 말할 때 사용한다.
---

# /start

작업을 시작하는 입구다. 다음 순서로 수행한다.

1. 현재 브랜치가 main 이 아니고 미커밋 변경이 남아 있으면, 먼저 `/deliver` 로
   정리할 것인지 사용자에게 확인한다.
2. main 으로 이동하고 `fetch` 와 `pull --rebase` 를 실행한다.
3. 사용자가 설명한 작업 내용으로 GitHub 이슈를 만든다. 제목과 본문과 라벨을
   `rules/issue-and-release.md` 의 형식에 맞춘다.
4. 발급된 이슈 번호로 `{type}/{issue-number}-{work}` 브랜치를 만들고 이동한다.
   `type` 은 작업 성격에서 판단하고, `work` 는 영문 소문자와 붙임표로 요약한다.
5. 만들어진 이슈 번호와 브랜치 이름을 사용자에게 보고한다.

이미 존재하는 이슈 번호를 사용자가 지정한 경우에는 3단계를 건너뛰고 그 이슈를
읽어 브랜치 이름을 만든다.

## 브랜치 이름 규칙

브랜치 이름 형식과 허용하는 타입 여섯 가지는 `rules/branching.md` 를 따른다.

## 이슈 형식

이슈 제목과 본문 구성과 라벨 체계는 `rules/issue-and-release.md` 를 따른다.

## 이슈 생성 방법

이슈는 `gh issue create` 로 만든다. 명령의 출력에서 발급된 이슈 번호를 읽어
브랜치 이름에 넣는다.

## 원격이 없을 때

원격 저장소가 연결되어 있지 않으면 `gh issue create` 가 실패해 이슈를 만들 수
없다. 이 경우 그 사실을 사용자에게 알리고 원격 연결을 요청한 뒤 멈춘다. 이슈
번호 없이 임의로 브랜치를 만들지 않는다.
