# CLAUDE.md

이 문서는 Claude Code 에이전트가 이 저장소에서 지켜야 할 행동 규칙을 요약하고,
세부 규칙 문서로 안내하는 색인이다.

## 이 저장소는 무엇인가

문서가 정해진 템플릿을 따르는지 검사하고, 맞지 않으면 거절하면서 쓸 템플릿을
안내하는 체계(**doc-guard**)의 엔진 저장소다. 세 가지를 담는다.

- `checker/` — 검사 엔진. 문서를 읽어 규칙 위반 목록을 낸다.
- `plugins/doc-guard/` — 엔진을 부르는 Claude Code 플러그인. 훅만 들어 있고
  엔진은 들어 있지 않다. 이 저장소 자체가 플러그인 마켓플레이스라 팀원은
  여기서 설치해 쓴다.
- `.github/workflows/doc-guard.yml` — 다른 저장소가 불러 쓰는 재사용 워크플로.
  GitHub Actions 경로는 이것을 통해 엔진을 가져다 쓴다.

### 저장소 셋

doc-guard 는 저장소 셋이 맞물려 돌아간다. 어디에 무엇이 사는지 헷갈리면 결함의
우선순위를 잘못 잡게 되므로 먼저 읽는다.

| 저장소 | 무엇이 사나 | 검사와의 관계 |
|---|---|---|
| **엔진 저장소** (이 저장소) | 검사 엔진, 플러그인, 재사용 워크플로 | 검사하는 **도구**를 제공한다 |
| **문서 저장소** | 회사별 폴더. 각 폴더가 `templates/` 와 `rules/` 와 `docs/` 를 든다 | 검사 **기준**을 제공한다 |
| **프로젝트 저장소** | 프로젝트마다 새로 생긴다. 실제 최종 문서 | 검사가 **여기서** 걸린다 |

프로젝트 저장소에서 PR 을 열면 검사가 시작되고, 그 자리에 셋이 모인다. 문서는
이미 그 저장소에 있고, 엔진은 엔진 저장소에서 내려받고, 기준은 문서 저장소에서
읽어온다. **문서가 사는 저장소와 기준이 사는 저장소가 다르다**는 것이 이 구조의
핵심이고, 엔진이 아직 그 가정을 반영하지 못하고 있다(아래 "지금 어디까지 왔나").

문서 저장소는 진행 중인 문서가 사는 곳이 아니다. 회사별 템플릿과 규칙의 원본을
두는 곳이고, `docs/` 는 끝난 프로젝트의 산출물을 자산으로 보관하는 자리다.
진행 중인 문서는 프로젝트 저장소에 있다.

### 건드리기 전에 알아야 할 것

1. **검사 엔진은 플러그인 밖에 둔다.** 같은 엔진을 플러그인 훅(팀원 PC 의
   Claude)과 GitHub Actions(GitHub 서버) 두 곳에서 부른다. Actions 는 팀원
   PC 에 설치된 플러그인을 쓸 수 없으므로, 엔진을 `plugins/` 안으로 옮기면
   Actions 경로가 끊긴다. 대신 **엔진을 얻는 경로가 관문마다 다르다**는 것을
   잊지 않는다. Actions 는 이 저장소에서 직접 내려받고, 훅은 플러그인을 통해
   받는다. 양쪽을 모두 챙겨야 하며, 지금은 훅 쪽이 끊겨 있다.
2. **규칙은 코드가 아니라 데이터다.** 검사 규칙은 고객사별 `rules.yaml` 로
   두고, 검사기는 규칙 종류를 모듈로 등록하는 구조로 만든다. 규칙이 늘어날
   때 엔진 코드를 고치게 되면 설계가 어긋난 것이다.
3. **기준은 검사할 때마다 문서 저장소에서 읽어온다.** 프로젝트 저장소에 기준을
   복사해 두지 않는다. 기준을 한 곳에서만 관리하기 위해서다. 대신 진행 중인
   프로젝트가 기준 변경에 흔들릴 수 있다는 약점을 안고 간다. 실제로 아파지면
   프로젝트마다 기준 버전을 적어 고정하는 방식을 얹는다.
4. **주 관문은 Actions 고 훅은 보조다.** 문서는 팀원이 자기 PC 에서 오피스
   프로그램으로 만드는 경우가 많고, 그때는 훅이 불리지 않는다. 검사가 반드시
   걸려야 하는 지점은 저장소에 올리는 순간이다. 훅은 Claude 로 쓰는 문서를
   더 이른 시점에 잡아주는 보조 장치로 둔다.
5. **문서를 올리는 사람은 문서를 쓴 프로젝트 팀원이다.** 거절당하면 쓴 사람이
   바로 고치는 것이 가장 짧은 고리다. 고객사가 직접 올리는 경로는 아직 범위
   밖이다.
6. **문서 버전은 파일명에 남긴다.** `개발사양서_v1.0.xlsx` 처럼 적고 이전 버전
   파일도 지우지 않고 함께 둔다. git 이 이력을 들고 있지만, 고객사에 몇 번째
   판을 넘겼는지가 폴더만 봐도 보여야 한다.
7. **"검사를 못 했다" 를 "통과" 나 "위반" 으로 뭉개지 않는다.** 막는 체계에서
   가장 위험한 실패는 고장이 눈에 보이지 않는 것이다. 검사를 수행할 수 없으면
   통과시키지 말고, 위반이라고도 하지 말고, 검사를 못 했다고 말한다.

### 지금 어디까지 왔나

검사 엔진이 동작한다. 규칙 종류 일곱(파일명, 위치, 필수 절, 필수 시트, 표 헤더, 슬라이드
레이아웃, 외부 흔적)과 리더 넷(md, docx, xlsx, pptx), `check` CLI 가 들어 있다.

외부 흔적 규칙만 묻는 것이 다르다. 나머지는 "이 회사 템플릿과 룰대로 썼나" 를 보지만
그것은 "이거 내보내도 되나" 를 본다. 걷어내는 것은 `--clean` 으로 사람이 직접 부른다.

**Actions 경로는 검증되었다.** 문서 저장소의 PR 에 일부러 위반 문서를 올려, 슬라이드
레이아웃 위반과 파일명 위반을 잡아 PR 을 실패시키는 것까지 확인했다.

**훅 경로는 아직 한 번도 발동한 적이 없다.** 플러그인 설치본에는 훅만 담기고 엔진이
따라가지 않아, 훅이 검사를 시작하기 전에 죽는다. 그리고 죽으면 조용히 통과시킨다.
한글 경로에서 회사 폴더를 찾지 못해 통과하는 결함도 함께 남아 있다.

**프로젝트 저장소 경로는 아직 없다.** 엔진은 검사할 문서 바로 위에서 회사 폴더를
찾는다. 문서와 기준이 다른 저장소에 있는 상황을 모르기 때문에, 문서만 있는 저장소에
붙이면 관할 밖으로 판단해 전부 통과시킨다. 이 구조에서 가장 큰 미완성이다.

열려 있는 결함은 GitHub 이슈로 관리한다. 우선순위는 프로젝트 저장소 경로와 검사 불능
구분이 앞이고, 훅 배선과 인코딩이 그다음이다.

### 아직 정하지 않은 것

정한 것과 정하지 않은 것을 섞지 않기 위해 남겨 둔다.

- **회사가 늘어날 때의 접근권한 분리.** 지금은 문서 저장소 하나에 회사별 폴더를
  둔다. 한 고객사 자료를 다른 고객사 관계자가 보면 안 되는 상황이 생기면 저장소를
  나누는 문제가 따라온다.
- **프로젝트 저장소에 검사를 붙이는 방법.** 저장소마다 워크플로 파일 한 장이
  있어야 검사가 돈다. 새 프로젝트마다 사람이 챙길지, 저장소 템플릿에 넣어 둘지,
  조직 차원에서 걸지 정하지 않았다.
- **제안의 범위.** 지금은 무엇이 틀렸는지 알리고 쓸 템플릿을 안내하는 데까지다.
  고치는 방법을 행동 단위로 알려주거나 고쳐진 파일을 만들어 주는 것은 나중 일이다.
- **비공개 문서 저장소를 읽을 권한.** 문서 저장소에 고객사 템플릿이 들어가므로
  공개로 둘 수 없다. 프로젝트 저장소의 Actions 가 그것을 읽을 방법이 필요하다.

## 규칙 색인

아래 다섯 문서가 이 저장소의 규칙 전문이다. 이 문서는 요약만 담고, 정확한
내용과 예시는 각 문서를 읽어야 한다.

| 문서 | 내용 |
|---|---|
| `rules/branching.md` | 브랜치 이름 형식, main 직접 커밋·푸시 금지, `/start` 로 브랜치를 만드는 절차 |
| `rules/commit-and-pr.md` | 커밋 메시지와 PR 제목·본문 형식, 푸시 전 fetch·rebase, `--force` 금지 |
| `rules/issue-and-release.md` | 이슈 제목·본문·라벨 체계, 이슈 크기 기준, 릴리즈 버전과 노트 형식 |
| `rules/delegation.md` | 메인 세션과 서브에이전트의 역할 분리, 서브에이전트 모델 고정, 자기 승인 금지 |
| `rules/governance.md` | 리뷰·병합 방식, 스킬을 반드시 거쳐야 하는 작업 목록, 훅이 막았을 때의 대응 |

새 파일을 어디에 둘지는 위 "이 저장소는 무엇인가" 절의 디렉터리 설명을 따른다.

## 가장 중요한 세 가지

세부는 위 색인을 따라가면 되지만, 아래 세 가지는 항상 지킨다.

1. **main 에는 직접 커밋하지 않는다.** 작업은 항상 `/start` 로 이슈와 브랜치를
   먼저 만들고 시작한다.
2. **푸시는 `/deliver` 로 한다.** 스킬을 거치지 않은 맨손 `git push` 는 훅이
   거부한다.
3. **조사와 실행은 `sonnet` 서브에이전트에 맡긴다.** 메인 세션은 무엇을
   조사할지 정하고 결과를 판정하며, 실제로 파일을 읽고 명령을 실행하는 부분은
   서브에이전트가 수행한다.

## 스킬

| 스킬 | 언제 쓰는가 |
|---|---|
| `/start` | 새 작업을 시작할 때. 이슈를 만들고 규칙에 맞는 브랜치를 만들어 준다 |
| `/deliver` | 작업을 마칠 때. 커밋과 기준 브랜치 동기화와 푸시와 PR 생성까지 한 번에 처리한다 |
| `/wrapup` | 세션에서 다 끝내지 못한 작업을 이슈로 남길 때 |

`/release` 와 `/intake` 는 이 저장소에 두지 않았다. 릴리즈를 발행할 때가
되거나 외부 디렉터리를 적재할 일이 생기면 `jaeheeMin/public-cloud` 에서
가져온다.

## 세부는 `rules/` 를 읽는다

이 문서는 규칙의 전문을 옮겨 적지 않는다. 위 색인의 각 문서가 정본이고, 이
문서와 어긋나는 부분이 있으면 `rules/` 쪽을 따른다.
