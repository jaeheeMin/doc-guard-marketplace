# docs/ssot/(PRD) 를 바꾼 PR 을 승인할 수 있는 사람 목록이다(#49).
# 한 줄에 GitHub 아이디 하나. `#` 뒤는 주석이고, 아이디 앞에 `@` 를 붙여도 된다.
# 대소문자는 구분하지 않는다.
#
# 이 파일에 아이디가 하나도 없으면(주석과 빈 줄만 남으면) PR 작성자가 아닌
# 누구의 승인이든 인정한다.
{{ssot_approvers}}
