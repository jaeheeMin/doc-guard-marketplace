# {{project}} PRD

## 배경과 목표

아직 작성 전이다. `/harness:prd` 로 만든다.

## 범위

아직 작성 전이다. `/harness:prd` 로 만든다.

## AS-IS / TO-BE

아직 작성 전이다. `/harness:prd` 로 만든다.

## 요구사항

아직 작성 전이다. `/harness:prd` 로 만든다.

## 참조

아직 작성 전이다. `/harness:prd` 로 만든다.

## 미결 사항

아직 작성 전이다. `/harness:prd` 로 만든다.
